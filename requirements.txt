# Core dependencies — pin to exact versions for reproducible builds
# Regenerate with: pip-compile pyproject.toml
anthropic>=0.40.0
pyyaml>=6.0.2
pydantic>=2.7.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-dotenv>=1.0.1
python-multipart>=0.0.9

# Optional — uncomment to enable
# sentence-transformers>=3.0.0   # semantic mapping
# pypdf>=4.2.0                   # PDF support
# slowapi>=0.1.9                  # API rate limiting
